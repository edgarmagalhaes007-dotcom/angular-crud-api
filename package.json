{
  "name": "entrega-crud-ecommerce",
  "version": "1.0.0",
  "private": true,
  "description": "Validação da integração Angular/Express da atividade de e-commerce",
  "scripts": {
    "test:api": "node scripts/testar-api.mjs",
    "test:frontend": "npm test --prefix frontend -- --watch=false",
    "build": "npm run build --prefix frontend"
  },
  "engines": { "node": "^22.22.3 || ^24.15.0 || >=26.0.0" },
  "devDependencies": { "newman": "6.2.1" }
}
